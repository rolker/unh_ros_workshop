# Day 2 of the ROS Bootcamp

Today we learned about basic ros nodes including publishing and services.

When we moved to incorporating day 1's vision components, we encountered compatibility issues between ROS and Python virtual environments. These issues are more challenging to resolve in Ubuntu 24.04 with ROS Jazzy. We're attempting to use a Docker container with Ubuntu 22.04 and ROS Humble, where pip packages can be installed without requiring a virtual environment.
